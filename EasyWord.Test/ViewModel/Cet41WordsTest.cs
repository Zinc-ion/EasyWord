using EasyWord.Library.Pages;
using EasyWord.Library.Services.Impl;
using EasyWord.Library.Services;
using Moq;

namespace EasyWord.Test.ViewModel;

public class Cet41WordsTest
{   /**
    [Fact]
    public async void KnowWord_Default()
    {
        var mockStorage = new Mock<IWordStorage>();
        var viewModel = new Cet4_1Words(mockStorage.Object);
        viewModel.KnowWord(2);
        Assert.Equal(1, await viewModel.KnowWord(2)); // 验证状态更新
    }

    [Fact]
    public async void UnknownWord_Default()
    {
        var mockStorage = new Mock<IWordStorage>();
        var viewModel = new Cet4_1Words(mockStorage.Object);
        viewModel.UnknownWord(2);
        Assert.Equal(1, await viewModel.UnknownWord(2)); // 验证状态更新
    }**/
}